   # dependencies
   /node_modules
   /.pnp
   .pnp.js

   # testing
   /coverage

   # next.js
   /.next/
   /out/

   # production
   /build

   # misc
   .DS_Store
   *.pem

   # debug
   npm-debug.log*
   yarn-debug.log*
   yarn-error.log*

   # local env files
   .env*.local
   .env

   # vercel
   .vercel

   # typescript
   *.tsbuildinfo
   next-env.d.ts

# 用记事本或其他编辑器打开 .gitignore，添加以下内容
node_modules/
.next/
.env
.env.local

echo "mytoolnest/node_modules/@next/swc-win32-x64-msvc/next-swc.win32-x64-msvc.node" >> .gitignore
git add .gitignore
git commit -m "Remove large file and add to .gitignore"
